# TruEstate Backend

Express API for Retail Sales Management System.

## Scripts

- `npm run dev` – start in watch mode
- `npm start` – start in production mode

## Endpoint

- `GET /api/sales` – supports search, filters, sorting, pagination via query params.
